<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/card_spacing"
    android:layout_marginBottom="@dimen/card_spacing">
    <androidx.cardview.widget.CardView
        android:layout_width="@dimen/round_card_size"
        android:layout_height="@dimen/round_card_size"
        android:layout_marginLeft="@dimen/card_spacing"
        android:layout_marginTop="@dimen/card_spacing"
        android:layout_marginRight="@dimen/card_spacing"
        app:cardBackgroundColor="@color/colorCategoryCardBackground"
        app:cardCornerRadius="@dimen/round_card_radius"
        app:cardElevation="@dimen/cardElevation">

        <ImageView
            android:id="@+id/cat_img"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/img_default_description"
            android:layout_gravity="center"
            />
    </androidx.cardview.widget.CardView>
    <TextView
        android:id="@+id/cat_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:fontFamily="@font/roboto_italic"
        android:textColor="@color/colorCategoryTitle"
        android:textSize="@dimen/text_size_category_name"
        android:textStyle="bold"/>
</LinearLayout>